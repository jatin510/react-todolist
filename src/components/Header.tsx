import React from 'react';

function Header() {
  console.log('header rendered');
  return (
    <header className="App-header">
      <h1>To Do List</h1>
    </header>
  );
}

export default Header;
